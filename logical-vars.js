/*
1.
const testValues = [true, false];

testValues.forEach(test => {
    console.log(test ? 'Верно' : 'Неверно');
});

testValues.forEach(test => {
    if (test) {
        console.log('Верно');
    } else {
        console.log('Неверно');
    }
});

2.
testValues.forEach(test => {
    console.log(!test ? 'Верно' : 'Неверно');
});

testValues.forEach(test => {
    if (!test) {
        console.log('Верно');
    } else {
        console.log('Неверно');
    }
});
*/
